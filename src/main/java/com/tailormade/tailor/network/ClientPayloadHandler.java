package com.tailormade.tailor.network;

import net.neoforged.neoforge.network.handling.IPayloadContext;

public class ClientPayloadHandler {
//    public static void handleLandRegionSync(final LandRegionSyncPayload payload, final IPayloadContext context) {
//        context.enqueueWork(() -> {
//            ClientLandCache.updateCache(payload.hash(), new HashSet<>(payload.positions()));
//        });
//    }
}
